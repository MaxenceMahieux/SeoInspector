import {HeadingIcon} from "../../components/Icons.tsx";
import SectionHeading from "../../components/SectionHeading.tsx";

function Audit() {
    return (
        <div>
            <SectionHeading icon={<HeadingIcon />} title={`Headings`} />
        </div>
    )
}

export default Audit;
